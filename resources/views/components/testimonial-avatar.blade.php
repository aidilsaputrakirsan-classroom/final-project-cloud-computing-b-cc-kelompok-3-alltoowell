<div class="flex items-center gap-4">
    <div class="w-16 h-16 gradient-blue rounded-2xl flex items-center justify-center shadow-lg">
        <span class="text-2xl font-bold text-white">{{ $label }}</span>
    </div>

    <div>
        <p class="font-bold text-gray-900 text-lg">{{ $name }}</p>
        <p class="text-sm text-gray-500">{{ $major }}</p>
        <p class="text-xs text-primary font-semibold mt-1">Mahasiswa ITK</p>
    </div>
</div>
